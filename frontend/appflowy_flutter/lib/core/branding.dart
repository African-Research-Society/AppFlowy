/// Brand constants for the ARS Workspace fork.
///
/// Display-facing name only; internal identifiers (bundle ids, package
/// names, data-folder names) are configured per platform.
const kBrandName = 'ARS Workspace';
